# Núcleo de detecção / anonimização (Microsoft, MIT)
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364

# NLP local
spacy==3.8.16

# NER transformer local.
# torch NAO entra aqui: e instalado no Dockerfile a partir do indice CPU
# (ou CUDA, via ARG TORCH_INDEX) para nao arrastar 2.5 GB de CUDA sem necessidade.
transformers==4.57.1

# PDF
PyMuPDF==1.28.2
pdfplumber==0.11.10

# Corpus sintético
Faker==40.37.0

# Interface web (Fase 1)
fastapi==0.121.2
uvicorn==0.41.0
python-multipart==0.0.20

# Testes
pytest==8.4.2
httpx==0.28.1

# Persistencia (Fase 4) — identidade, inquilino e trilha.
# NUNCA documento: ver goal-fase-4.md secao 1.
#
# Licencas conferidas em 2026-09-05 contra os metadados do PyPI, porque o
# CLAUDE.md secao 6 exige verificar antes de adicionar e a lista de pendencias
# ja tem RN-04 e RN-05.
#
#   SQLAlchemy   MIT
#   alembic      MIT
#   pg8000       BSD 3-Clause
#   argon2-cffi  MIT
#
# pg8000 e nao psycopg2-binary: aquele e "LGPL with exceptions". O copyleft
# quase certamente nao morderia — biblioteca importada dinamicamente e
# substituivel —, mas "quase certamente" e o que a regra manda recusar quando
# existe alternativa permissiva sem custo. pg8000 e Python puro, e dialeto
# oficial do SQLAlchemy, e a carga aqui e autenticacao e auditoria, nao vazao.
SQLAlchemy==2.0.52
alembic==1.19.2
pg8000==1.31.5
argon2-cffi==25.1.0
